databases:
  - name: cardapio-online-db
    plan: free
    databaseName: cardapio
    user: cardapio

services:
  - type: web
    name: cardapio-online-backend
    runtime: node
    rootDir: backend
    plan: free
    # TEMPORÁRIO: roda o backfill de entregueEm uma vez, depois da migration e
    # antes do build. Remover a cláusula "&& npm run db:backfill-entregue-em"
    # assim que os pedidos ENTREGUE legados estiverem confirmados com entregueEm.
    buildCommand: npm install && npx prisma migrate deploy && npm run db:backfill-entregue-em && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cardapio-online-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: https://cardapio-online-frontend-hgrc.onrender.com
      - key: NODE_ENV
        value: production

  # Site estático: type continua "web", quem diferencia é runtime: static.
  # Sem campo "plan" — sites estáticos no Render não têm plano pago/gratuito,
  # são sempre grátis, e declarar "plan" aqui (mesmo "free") é inválido.
  - type: web
    name: cardapio-online-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://cardapio-online-backend-odg7.onrender.com
    routes:
      # SPA: qualquer caminho (/painel/login, /pedido/5, etc.) precisa servir o
      # index.html — quem decide a rota é o react-router, no navegador.
      - type: rewrite
        source: /*
        destination: /index.html
